import { Box } from "@mui/material";
import React from "react";

const Dashboard = () => {
  return (
    <Box className="container-content " sx={{ backgroundColor: "cyan" }}>
      Dashboard
    </Box>
  );
};

export default Dashboard;
